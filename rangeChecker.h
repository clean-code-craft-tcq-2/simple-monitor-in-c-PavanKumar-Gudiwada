#include <stdbool.h>

extern bool isRangeHigh(float paramValue, float upperLim);

bool isRangeLow(float paramValue, float lowerLim);

bool isParamInRange(float paramValue, float upperLim, float lowerLim);
